import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ccff-angular-app',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
